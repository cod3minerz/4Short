# Caddy's production environment. Copy to the server-only environment file;
# never commit a file containing provider or application secrets.
ACME_EMAIL=ops@hashpix.ru
API_DOMAIN=api.hashpix.ru
# Keep the legacy host while 4short.ru is redirected to Hashpix. Remove it
# only after the migration window and the old certificate are no longer needed.
LEGACY_API_DOMAIN=api.4short.ru
