---
name: backend-capability-map
description: Living inventory of what the 4Short backend (control-api + media-worker + contracts) actually implements versus what's only a type, a UI mock, or a dead schema field. Use before designing ANY dashboard UI for a feature you haven't personally verified — video formats, subtitles, sources, plans, billing, socials. Keywords: does the backend support, is this real, backend capability, ClipLayout, StyleConfig, exists or not.
---

# Backend capability map

Read this before designing UI for a feature. It exists because the previous redesign pass built wizard/editor UI for several things that were never wired to any backend — the failure mode this skill prevents.

**This file goes stale.** Whenever you change a route, a worker stage, or a contract, update the relevant row here in the same change.

## The most severe finding so far: no frontend code ever provisioned a workspace

Every authenticated `control-api` route requires an `X-Workspace-Id` header (`requireWorkspace` in `services/control-api/src/plugins/context.ts` throws a hard 400 if it's missing — no auto-resolution, no "use the user's only workspace" fallback). The dashboard reads that header's value from `localStorage` via `getWorkspaceId()`/`setWorkspaceId()` (`app/dashboard/lib/control-api.ts`). A real, complete backend endpoint already existed to create one — `POST /v1/onboarding/workspace` (`services/control-api/src/routes/onboarding.ts`), which seeds free-trial minutes and a default style — but **`setWorkspaceId()` was never called anywhere in `app/**`, and nothing ever called the onboarding route either.** In any real, connected deployment, a brand-new user could sign in successfully (auth works fine) and then every single subsequent API call — billing, projects, styles, everything — would fail with a 400, because no workspace ID was ever stored. This is more severe than any single-page bug found this session: it would make the entire product unusable for every real user, at the very first step.

Fixed (E-AUDIT) by: (1) making `POST /v1/onboarding/workspace` idempotent by user, not just by client-supplied idempotency key — it now looks up an existing `workspaceMembers` row for the signed-in user first, and only falls through to creating a new workspace if none exists, so a returning user (new device, cleared storage) lands back on their own workspace instead of getting a disconnected duplicate; (2) `ensureWorkspace()` added to `control-api.ts`; (3) `store.ts`'s `hydrate()` now resolves the session first (on its own, not in the `Promise.all`), and if there's a real signed-in user with no stored workspace ID, calls `ensureWorkspace()` and `setWorkspaceId()` *before* running any of the workspace-scoped fetches; (4) `signOut()` now clears the stored workspace ID too, so a second user signing in on the same browser doesn't inherit the first user's (inaccessible) workspace. If you touch workspace resolution again, re-verify this chain end-to-end — it can't be exercised in this repo's disconnected dev preview (`isControlApiConfigured()` is false here), so it was verified by tracing the code against `services/control-api/src/plugins/context.ts`, `tsc`, `next build`, and the backend's own `tsc`, not by a live click-through.

### A second, deeper part of the same gap: there was no sign-in UI anywhere

Even with workspace provisioning fixed, nothing in `app/**` ever triggered a sign-in — no email/OTP form, no OAuth button, `getSession()`/`signOut()` were the only auth-related frontend code, both of which just assume a session already exists. The backend (`services/control-api/src/auth/index.ts`) is fully configured for real email-OTP sign-in via `better-auth`'s `emailOTP` plugin (`emailAndPassword` is explicitly disabled) plus optional Yandex OAuth — but nothing called it. Fixed (E-AUDIT) by adding a real sign-in page at `app/sign-in` (deliberately **outside** `app/dashboard/**`, as a new top-level route, so it isn't wrapped in `DashboardShell` and doesn't touch `app/page.tsx`/`app/components/*`):
- `app/sign-in/page.tsx` + `sign-in.css` — a two-step email → OTP-code form. Calls `sendSignInOtp`/`verifySignInOtp` (new in `control-api.ts`), hitting `POST /v1/auth/email-otp/send-verification-otp` and `POST /v1/auth/sign-in/email-otp` — real endpoint paths and body shapes, confirmed by reading `node_modules/better-auth/dist/plugins/email-otp/routes.mjs` directly rather than guessing. On success, redirects to `/dashboard` (or a `?redirect=` target), where the newly-fixed `hydrate()` picks up the fresh session and provisions the workspace automatically.
- **Yandex OAuth is intentionally not wired** — it's conditionally configured server-side (`YANDEX_CLIENT_ID`/`YANDEX_CLIENT_SECRET`) and the frontend has no way to know if it's actually enabled without a new endpoint; email-OTP alone fully closes the "nobody can sign in at all" gap, so this was left as a known, smaller remaining item rather than scope-creeping further.
- `dashboard-shell.tsx` now redirects to `/sign-in?redirect=<path>` when `connection === "connected" && !user` — i.e. only once the real backend has actually answered and confirmed there's no session; the `connection === "preview"` (API not configured) state never redirects, since that's a deliberate offline-demo mode, not a signed-out one.
- None of this is exercisable live in this repo's disconnected dev preview; verified via `tsc`, `eslint`, `next build`, and reading the real better-auth plugin source for exact contracts.

**Caught by a fresh review agent, not by me: the sign-in fix above shipped with a redirect loop that would have made it completely unusable.** `store.ts`'s `hydrate()` is guarded by a module-level `hydrated` flag set `true` before the first `await` and *never reset*. The redirect-to-sign-in itself (in `dashboard-shell.tsx`) is what triggers the first hydration, permanently caching `user: null`. After a real user completed the OTP flow, `router.push("/dashboard")` (a soft navigation) didn't reload the module, so `hydrate()` no-opped on the stale `hydrated=true`, the store kept reporting `user: null`, and the redirect fired again — sending a just-authenticated user straight back to `/sign-in`. `signOut()` already avoided this exact trap by using `window.location.href` (a hard reload, which does reset module state) instead of a router push — that asymmetry is what hid the bug from a sign-out-only test. Fixed by making the post-verify redirect in `app/sign-in/page.tsx` a hard navigation too (`window.location.href`, not `router.push`). **Lesson for next time:** any code path that changes what `getSession()`/`getWorkspaceId()` would return must navigate with a hard reload, not `router.push`/`router.replace` — the dashboard store's hydration is a one-shot-per-page-load singleton by design (mirrors `styles`/`billing`/`projects`, all fetched once and cached), so client-side navigation across an auth-state change will always see stale data.

**Also caught in the same pass, pre-existing (not something this session's other changes introduced):** the per-moment "Кадр" override in `project-workspace.tsx`'s moment inspector (a raw `<select>`, not using the shared `Select`/`LockedField` primitives) called `updateMoment(..., { layoutOverride: {...} })`. `updateMomentSchema` (`packages/contracts/src/api.ts`) accepts and validates `layoutOverride` and `speakerId`, the request returned 200 with no error — but `momentCandidates` (`db/schema.ts`) has no column for either field, and the PATCH handler (`services/control-api/src/routes/projects.ts`) never wrote them. The UI showed the change as saved; on reload it silently reverted, because the API-moments mapping in the same file hardcodes `layout: "auto"` for every moment regardless of what was picked. Fixed by locking the control (`LockedField`, "Свой формат для отдельного момента — пока недоступно, используется формат проекта") and removing the now-dead `layoutOverride` plumbing from `updateActive()`/the `layouts` option list, rather than attempting a schema migration to actually persist it — that's a real data-model change (new columns + a `db:generate`/`db:migrate` this sandbox has no live database to verify against), out of proportion for this pass. `speakerId` on the same schema has the identical "accepted, nowhere to store it" gap but was never actually reachable from any UI control, so it wasn't touched.

**Small adjacent fix, found while verifying the above:** `services/control-api/src/app.ts`'s CORS `allowedHeaders` list was missing `X-Development-User-Id` — the exact header the frontend sends (and `plugins/context.ts` + `routes/onboarding.ts` read) for the local dev-mode auth bypass (`NEXT_PUBLIC_DEVELOPMENT_USER_ID`). Since the dashboard's default dev ports differ from the API's (`:3000`/`:3001` vs `:4100`), this is a real cross-origin request and the browser's CORS preflight would have silently stripped/blocked the header, making the dev-user-id bypass non-functional whenever frontend and backend run on different origins — which is the default setup. Added to the allowlist.

## Critical: `transcript_segments` was never written at all — the transcript feature had zero real data

Found while reading `app/dashboard/lib/transcript.ts`'s `toEditorWords()` — its own comment already flagged "nothing in this repo writes it yet" for the `.words` column, which prompted checking one level up: does anything write `transcript_segments` *rows* at all? It didn't. `pipeline.ts`'s `speech_to_text` completion handler wrote the raw STT provider response into `transcripts.originalPayload` (an opaque jsonb blob) and stopped there — nothing ever parsed it into `transcript_segments` (ordinal/speakerId/startMs/endMs/originalText/words). `GET /v1/projects/:id/transcript` does a plain `select from transcript_segments where transcriptId = ...` with no fallback, so for every real, connected project this always returned an empty array. That silently broke: the clip editor's transcript panel, `project-workspace.tsx`'s transcript drawer (both fixed earlier this pass to *use* real segments once fetched — this is the reason there was nothing to fetch), word-level editing (`toEditorWords`), and by extension `transcript_revisions`' hide/cut/replace-word operations (already documented above as write-only/never-replayed — now clear that was compounding on top of segments that were never there to begin with).

Fixed for one of the two configured STT providers: `services/control-api/src/services/transcript.ts` (`parseSttResponse`) parses the OpenAI-Whisper `verbose_json` shape (`OpenAICompatibleStt` in the worker) — a stable, well-documented format — into segment rows, bucketing any top-level `words[]` timings into their containing segment by time range. Wired into `pipeline.ts`'s `speech_to_text` handler (delete-then-reinsert on each transcription). **Deliberately not fixed for `YandexSpeechKit`, and — important — `yandex_speechkit` is the worker's DEFAULT `stt_provider`** (`services/media-worker/src/fourshort_worker/config.py:34`), not a rare alternate path. Its async-recognition response is a materially different, more deeply-nested shape (per-channel/per-chunk alternatives) that this pass could not verify against a live response or any fixture in the repo — guessing at the field names risked silently writing *wrong* segments to a real database, which is worse than the current empty state (wrong data actively misleads a user editing their transcript; empty data is at least honestly absent and the existing empty-state UI handles it). **This means the fix above only takes effect if a deployment explicitly overrides `STT_PROVIDER=compatible`** — on the default config, `transcript_segments` is still never populated. Whoever picks this up next should get one real Yandex SpeechKit v3 `recognizeFileAsync` response (either from a live call or Yandex's own API reference) and extend `parseSttResponse` in `services/control-api/src/services/transcript.ts` with a second branch for it, verified against that real shape rather than assumed. Real unit tests added: `tests/unit/stt-transcript-parsing.test.ts`.

## A systematic sweep of every request-body schema (E-AUDIT), after the moment-layoutOverride finding above turned out not to be isolated

Prompted by the `updateMomentSchema.layoutOverride`/`speakerId` finding (a field validated, accepted, 200-returned, but never written to any column), a background agent traced every request-body zod schema in `packages/contracts/src/*.ts` to its route handler(s) and downstream consumers, looking for the same "silently inert" pattern. Two more found:

- **`createProjectSchema.projectOverrides` — real bug, fixed.** The wizard's format-picker step sends this (`new-project-wizard.tsx`: `projectOverrides: { layout: layoutToApi(layout) }`), meaning **every project's per-project format choice from the wizard was being silently ignored at render time** — `services/control-api/src/services/pipeline.ts`'s `find_moments`→`face_track` handler built the EDL purely from `clip.style.config`, never merging in the project's stored `projectVersions.settings.projectOverrides`. The value *was* being written to a real column (so it's a step removed from a pure schema-only ghost field), just never read back — functionally identical to data loss from the user's point of view: pick "Размытый фон" in the wizard, every clip renders with whatever the linked *style's* layout says instead. Fixed by fetching the project's current `projectVersions.settings.projectOverrides` in the `face_track` handler and shallow-merging it over the style config before building the EDL (shallow merge is correct for the one field ever actually sent — `layout` — don't extend to a deep merge without a second field in real use to verify against).
- **`createMomentSearchSchema.resultMode` — confirmed real gap, left undone.** `"replace"` is meant to clear prior search results before inserting new ones; nothing in `pipeline.ts`'s `find_moments` completion handler branches on it at all — old `moment_candidates` from earlier searches always remain selectable regardless of which value is sent. **Currently unreachable in practice**: the only real call site (`project-workspace.tsx`'s `recompute()` / "Найти иначе") always sends `resultMode: "append"`; `"replace"` is never actually requested by any UI flow today. Not fixed — implementing real replace semantics needs a product decision this pass can't make blind (delete old `moment_candidates` outright vs. soft-hide vs. deselect; `clips.momentCandidateId` is `onDelete: "set null"` so a hard delete wouldn't destroy already-rendered clips, but would orphan their moment reference) and there's no live UI path to verify a fix against, unlike the `projectOverrides` case above. If a "replace"-triggering control is ever added to the UI, implement this then, with that real call site's exact intent to build against.

Two other schemas were checked and are genuinely unreachable/unfinished rather than broken, not urgent: `generativeQuoteSchema`/`confirmGenerativeOperationSchema` are defined but **no route parses either one at all** — `generative_operations`/`generative_balance_transactions` tables exist with zero API surface, consistent with AI B-roll/music being fully absent per the row below. Everything else checked out clean (`updateProjectSchema`, `createTranscriptRevisionSchema`, `updateClipSchema`, `reserveMinutesSchema`, style/upload/admin/job schemas).

## Verified as of the July 2026 mechanics-doc pass (cite file:line before trusting, code moves)

| Feature | Status | Where |
|---|---|---|
| YouTube import | Real | `services/media-worker/src/fourshort_worker/stages.py:65-122` (yt-dlp) |
| VK / RuTube / Twitch import | **Real** — whitelist widened, contract validation updated | `stages.py:68`, `packages/contracts/src/api.ts` `SUPPORTED_SOURCE_HOSTS` |
| Yandex/Google Disk import | Not planned — needs OAuth | — |
| oEmbed metadata (title/thumbnail) | Real, YouTube only, no key needed | `services/control-api/src/routes/media.ts` |
| Duration via YouTube Data API | Real, only if `YOUTUBE_API_KEY` set | `routes/media.ts:56-70` |
| Cheap non-AI clip-count estimate | Real, client-side arithmetic | `new-project-wizard.tsx` |
| HVE v2 time map / planned captions | **Internal execution path is real but not publicly enabled.** It builds an integer-microsecond output clock from narrative segments and source-time removals, derives conservative word-gap pause removals, maps captions to that clock, and serializes deterministic SRT/VTT. A pause-only crossfade is explicit as `transitionInUs` on the incoming time-map entry: output ranges overlap by that exact bounded duration, user/text cuts remain hard cuts, and the worker executes matched FFmpeg video/audio fades before visual composition. A source reviewer uses two synchronised native media elements only for this explicit overlap and canvas alpha blending; it labels an unavailable second audio element rather than claiming A/V parity. For a persisted V2 rerender, the control API resolves an immutable one-source 1× plan and the worker uses the same map for A/V timeline, output-timed ASS and bounded loudness normalization while recording child-process telemetry. The initial-project adapter supports only the safe source-only subset (`auto`/`static_crop`/`blur_background`, captions and text title, no asset layers), and is held behind `HVE_INITIAL_DOCUMENTS_ENABLED=false` until the public focus editor can use typed drafts. The current local macOS smoke lacks libass and correctly reports insufficient caption burn-in evidence; CI requires libass, but corpus visual gates are still missing. No UI may claim that v2 pause removal is live yet. | `packages/contracts/src/hve-time-map.ts`, `services/control-api/src/services/hve/initial-document.ts`, `services/control-api/src/services/hve/render-plan.ts`, `services/control-api/src/services/pipeline.ts`, `services/media-worker/src/fourshort_worker/render.py`, `services/media-worker/tests/test_render.py` |
| HVE v3 slot compositor / internal B-roll | **A narrow executor is real but not publicly enabled.** A data-driven registry resolves a layout to rounded canvas slot geometry, and the worker compiles that resolved geometry without layout-name branches. Full-decode fixtures prove two independent source crops render into separate output regions and that two contiguous output-clock layouts really switch source crop at the declared boundary. It accepts one or more contiguous, full-clip source-only layout segments; each segment can have a planner-owned, constant-size crop position trajectory, and the worker composes them separately then hard-cuts them back onto the shared output clock. Size changes, gaps, crossfades that cross a layout boundary or use a moving crop, incomplete coverage and all unverified track references fail closed. A pause crossfade is allowed only inside one static resolved layout and uses the shared HVE-2 A/V timeline before composition. It does not infer a face or active speaker. V2 text titles are resolved to output geometry and burned via ASS. Before planning executable foreground layers, an output-clock, ASS-compatible subtitle safe envelope is reserved for every active cue: `collisionPolicy: "move"` can move a colliding title/image/logo/banner/video only to a verified canvas edge, while `warn` and unresolved `move`/`shrink` leave the authored geometry intact and emit a machine-readable warning. This is deliberately an envelope-based planner warning, not a pixel/glyph visual guarantee; B-roll is excluded because captions intentionally render above it. Static `image`/`logo`/`banner` layers have a private asset pipeline: signed multipart upload, server-side byte signature/size/SHA-256 verification, workspace-scoped planning and worker-side re-verification before FFmpeg overlay; no object URL enters the document or canonical render plan. A `video` asset has a separate, real but intentionally narrow path: MP4 at most 100 MiB is worker-verified by H.264/yuv420p profile and full decode before planning; it may render only as a muted, output-clock visual overlay with range/duration checks and global z-order across static/timed layers. The internal `broll` path is stricter: asset kind must match; it replaces only the complete visual canvas over one bounded, non-overlapping range; it cannot loop or mix its audio and preserves the narrative audio/time map. Partial, transparent, short or overlapping B-roll fails before queueing. It has executable pixel/audio fixtures but stays non-public until browser preview parity, costed generative operations and corpus evidence exist. `outro` remains locked because it needs independent end-of-timeline/audio semantics. The internal HVE draft commit preflights this path before it creates a version; initial pipeline selection is restricted to the source-only subset above, so it still does not enable 2/3/4-person, screen or gameplay output. | `packages/contracts/src/hve-layout.ts`, `services/control-api/src/routes/brand-assets.ts`, `services/control-api/src/services/hve/initial-document.ts`, `services/control-api/src/services/hve/render-plan.ts`, `services/control-api/src/routes/editor.ts`, `services/media-worker/src/fourshort_worker/render.py`, `services/media-worker/tests/test_render.py` |
| HVE browser preview projection | **Internal pure-plan primitive, not a browser renderer.** Given a resolved plan it derives one output instant’s source seek time, slot pixels, crop interpolation, active captions and active production-layer projection (including B-roll’s immutable asset identity, timing and full-canvas policy). It has no media/model access, refuses multi-source, missing-layout or incomplete-crop inputs, and is covered against an output cut so it cannot glide across removed source media. A real `SequencePlayer`/Canvas backend and browser-vs-FFmpeg frame comparison remain unimplemented; no UI may claim preview/final parity. | `packages/contracts/src/hve-preview.ts`, `tests/unit/hve-preview.test.ts` |
| HVE v4 server editor draft | **Internal persistence path is real; the public rollout remains disabled.** A V2 clip document plus clip/social metadata can be materialized into one workspace-scoped draft, edited by idempotent command batches with optimistic revision checks, and committed only after the HVE-3 planner accepts it. The focus editor now contains an internal adapter for the executable subset: it sends typed source-layout, caption, trim, word, audio/export and versioned text-title commands, then commits exactly one immutable render version. Unsupported layouts and asset controls visibly refuse rather than approximate. A browser IndexedDB snapshot preserves unsent intent only when its exact `clipId`/document hash/base version/revision still matches the server draft; it never attempts an unsafe automatic rebase. Metadata uses the same revision/audit log but is excluded from the media document hash; commit atomically versions it with the document, updates the visible clip fields and queues one render job. Saving a draft never queues work. Existing legacy clips without `documentV2` receive an honest conflict rather than a lossy conversion. `HVE_INITIAL_DOCUMENTS_ENABLED=false` remains mandatory until offline/two-tab and preview/final gates pass. | `app/dashboard/lib/hve-draft-sync.ts`, `app/dashboard/lib/hve-draft-recovery.ts`, `app/dashboard/components/clip-editor.tsx`, `packages/contracts/src/hve-editor-commands.ts`, `services/control-api/src/routes/editor.ts`, `db/schema.ts` `clip_drafts` / `clip_versions` / `editor_command_batches` |
| HVE v5 sparse source perception | **Internal, asynchronous and non-blocking.** After moments are ready, the worker may sample a source at 0.5 FPS, detect histogram cuts and compact YuNet face tracks, then writes one immutable JSON scene graph to private S3. It position-seeks to each requested sample rather than decoding and discarding every intervening source frame; decoded frame count is bounded by the configured sample cap, although VFR seek timing remains an explicit approximation. Face association is globally assigned from bounded motion-projected boxes rather than greedy track order; detector boxes are normalized and clipped in bounds before crossing the artifact boundary. A narrow topology classifier can only recommend `solo`, two-face split, grid-3 or grid-4 when concurrent, durable verified tracks exist; it warns and falls back on insufficient evidence. The clip-scoped `GET .../layout-recommendation` endpoint merely returns that advisory plan after independently re-verifying source/workspace/hash/orientation/engine-release/artifact SHA; it never changes a draft or queues media work. It persists only timings, normalized boxes, confidence and warnings — no decoded frames or embeddings. An editor commit requesting an explicit face crop track re-reads that private artifact under a byte limit and verifies its stored SHA-256, source identity/hash/geometry and exact active engine release before the planner maps it to the output clock. Missing or mismatched evidence is a conflict/fallback, never an invented crop. It deliberately creates no speaker turns, active-speaker associations, screen, slide or gameplay claims; the crop trajectory is constant-size only and is **not active-speaker tracking**. A failure does not change the project state or delay transcript/moment work. The local environment has no OpenCV runtime, so the real visual fixture is skipped locally and production confidence remains gated on CI/corpus evidence. | `packages/contracts/src/hve-perception.ts`, `packages/contracts/src/hve-director.ts`, `services/control-api/src/routes/editor.ts`, `services/control-api/src/services/hve/perception-artifact.ts`, `services/control-api/src/lib/s3.ts`, `services/media-worker/src/fourshort_worker/vision.py` `SparseSourcePerception`, `stages.py` `analyze_visual`, `pipeline.ts`, `tests/test_vision.py`, `tests/unit/hve-director.test.ts` |
| HVE browser proxy | **Real internal asynchronous derivative, not a user-visible re-encode setting.** After a successful source probe, only an incompatible browser source may enqueue a delayed, CPU-medium `generate_proxy` job. The worker streams a bounded 720p-max, 30fps H.264/AAC/yuv420p MP4 with faststart, re-probes it before upload and records process telemetry. The private proxy object is retained with the source and bound through `sources.proxyMediaId`; it never replaces the original, delays STT/moment work or changes minute accounting. Visual perception may use a live, non-deleted proxy as an optimization, but safely falls back to the immutable original when it is missing or expired. This is not a preview-parity guarantee and has no public UI control yet. | `services/media-worker/src/fourshort_worker/media.py` `create_browser_proxy`, `stages.py` `generate_proxy`, `services/control-api/src/services/pipeline.ts`, `config.py`, `packages/contracts/src/api.ts`, `packages/contracts/src/hve-v2.ts`, `services/media-worker/tests/test_proxy.py` |
| HVE execution ETA | **Internal, evidence-only and runtime-scoped.** The API can calculate a cost-normalized p50/p90 execution range after ≥6 exact job-type measurements or ≥12 matching job-class measurements, but only when one non-draining worker has refreshed a complete, pinned-OCI runtime fingerprint in the last two minutes. Each completed attempt carries the same fingerprint, which binds HVE/model/font/cgroup settings. During a rolling deploy, mixed active runtimes, missing image digest or insufficient matching history the endpoint returns `HVE_ETA_RUNTIME_UNCERTAIN` / `HVE_ETA_SAMPLE_SIZE_INSUFFICIENT`, never a blended estimate. It deliberately reports no queue-start time because weighted fair dispatch has no validated queue simulator yet. No public progress UI consumes it. | `services/media-worker/src/fourshort_worker/runtime_identity.py`, `worker.py`, `services/control-api/src/services/hve-eta.ts`, `routes/projects.ts` `/eta`, `job_attempts.metrics` |
| HVE class-aware worker admission | **Real scheduler safety primitive, not a throughput claim.** A worker claim is serialized through its lease row, observes active leases by resource class, and rejects a `cpu_heavy`/`cpu_medium` candidate when the declared class slot is occupied even if `maxConcurrentJobs` would permit another job. The selector can skip a saturated heavy candidate and lease a following compatible medium candidate. CPU-job admission takes the stricter of host-visible free memory and cgroup-v2 headroom; finite cgroup limits are advertised to the scheduler instead of misleading host RAM. FFmpeg decode/filter/encoder threads are explicitly clamped per invocation. A cancelled/lost lease stops FFmpeg's whole process group and prevents the old attempt from completing; non-interruptible Python provider/model work is only stopped at its next stage boundary. The operator-only 60-second benchmark refuses a non-drained/active worker and records cgroup CPU quota as well as memory, so a host run cannot become the 7.5-core production baseline by accident. CI runs the actual PostgreSQL claim/reclaim path for class capacity, duplicate concurrent claim and expired lease recovery. Benchmarked fairness, multi-worker recovery under process death and safe parallel process envelopes remain gated. | `services/control-api/src/services/hve-scheduler.ts`, `services/control-api/src/services/queue.ts`, `services/media-worker/src/fourshort_worker/resources.py`, `services/media-worker/src/fourshort_worker/benchmark.py`, `services/media-worker/src/fourshort_worker/media.py`, `services/media-worker/src/fourshort_worker/process.py`, `services/media-worker/src/fourshort_worker/worker.py`, `infra/worker/bin/4short-worker-deploy`, `tests/unit/hve-worker-benchmark-baseline.test.ts`, `tests/integration/queue-pg.test.ts`, `services/media-worker/tests/test_resources.py`, `services/media-worker/tests/test_process.py`, `.github/workflows/ci.yml` |
| HVE-9 sealed release approval | **Evaluator-only assembler and public-key verifier exist; release is intentionally not approvable yet.** The assembler hashes the exact reports and signs one immutable candidate Git SHA/OCI digest, aggregate corpus/metric facts, baseline result and runtime FFmpeg/model provenance with an evaluator private key. The verifier accepts that envelope only against active frozen thresholds, all required suites/strata and the approved Timeweb baseline. It returns `INSUFFICIENT` for absent/under-sampled evidence and `FAIL` for tampering or quality failure; scaffold thresholds never pass. No production or market-quality claim is currently justified because the licensed corpus, independent threshold freeze, Timeweb baseline, chaos run and blinded comparison are not available. | `scripts/hve/assemble-production-release-evidence.mjs`, `scripts/hve/validate-production-release.mjs`, `verification/hve/thresholds/production-v1.json`, `verification/hve/schemas/release-evidence.schema.json`, `tests/unit/hve-production-release.test.ts`, `verification/hve/README.md` |
| HVE director | **Internal pure recommendation layer only.** It can recommend split, screen/presenter, gameplay/facecam or grid layouts only when a source graph already contains overlapping verified region tracks of the required kinds; otherwise it emits a safe portrait/blur fallback with a visible decision trace. It applies a deterministic 1.2–1.8s transient-layout hold: a prior composite is held only if its exact verified regions still cover the interval; otherwise the decision visibly falls back. This prevents plan-level layout flicker without renderer-side heuristics. The current perception job does not provide screen/gameplay/facecam regions, so it cannot recommend those layouts in a real project yet. It is not attached to normal project rendering or a dashboard control. | `packages/contracts/src/hve-director.ts`, `tests/unit/hve-director.test.ts` |
| HVE-6 user-verified composites | **Real internal plan primitive, not automatic recognition or a public control.** A single-source `gameplay_facecam`, `screen_speaker` or PiP layout can be assembled from a user-selected screen crop plus an exact dense face-track artifact. An explicit `grid_3`/`grid_4` can likewise bind each visible panel to a distinct user-selected verified face track. Typed draft commands carry only the user-confirmed crop/track identities and derive all executable slots from the immutable document, rather than accepting browser-supplied source bindings. The generic compositor draws all slots without a layout-specific render branch. Before the planner accepts any tracked slot it rechecks that the persisted manifest hash binds the exact private scene-graph artifact, certifies dense evidence covering every retained source interval, and verifies that each selected face track itself has source-clock/keyframe coverage for the complete retained clip; sparse, legacy, partial or vanished-track evidence fails closed. It deliberately cannot call a screen/gameplay detector or automatically assign panel roles yet, so no automatic streamer/panel layout is advertised. | `packages/contracts/src/hve-layout.ts` `buildUserVerifiedScreenCompositeSlots` / `buildUserVerifiedFaceGridSlots`, `packages/contracts/src/hve-editor-commands.ts`, `services/control-api/src/services/hve/perception-artifact.ts`, `services/control-api/src/services/hve/render-plan.ts`, `tests/unit/hve-render-plan.test.ts`, `services/media-worker/tests/test_render.py` |
| HVE-G6 automatic layout evaluation | **Evaluator-only scoring and signature chain exist; no automatic layout is approved.** A G6 range must independently label semantic `screen`/`gameplay`/`face`/`facecam` region roles. Evaluator-signed labels and model predictions bind the exact corpus index, annotations, candidate-to-semantic mappings, detector/director hashes and target hardware. The pure scorer derives per-stratum recall/layout accuracy, forbidden-layout rate, important-region preservation loss, transition p95 and RSS/swap facts; its benchmark report is also Ed25519 signed, and promotion refuses a missing or modified signature. Threshold promotion further requires ≥24 independently sourced items and ≥120 independently labelled ranges for each screen-presenter, gameplay-facecam and panel stratum. No private corpus, evaluated model or target-worker report exists yet, so HVE-G6 stays scaffolded and `HVE_AUTOMATIC_LAYOUT_DIRECTOR_ENABLED` must remain false. | `scripts/hve/layout-director-evaluation.mjs`, `scripts/hve/evaluate-layout-director-benchmark.mjs`, `scripts/hve/validate-layout-director-benchmark.mjs`, `scripts/hve/sign-evaluator-bundle.mjs`, `tests/unit/hve-layout-director-evaluation.test.ts`, `tests/unit/hve-layout-director-benchmark.test.ts`, `verification/hve/README.md` |
| HVE-G6 CPU layout baseline | **Evaluator-only candidate, not a live detector or product switch.** It runs on licensed corpus files only, verifies source/model bytes, samples video at a bounded rate, emits opaque MediaPipe face candidates plus OpenCV structural-rectangle candidates and makes a topology-only `screen_speaker`/`grid_3`/`grid_4`/safe-fallback decision. A separate builder derives signed-evidence-ready predictions only from hash-bound artifacts, evaluator-owned candidate-to-semantic maps, exact labelled ranges and cgroup-v2 target-hardware facts. A candidate rectangle is never automatically a “screen,” and this has no media-worker or control API invocation. It is a comparison baseline for the G6 corpus, not evidence that its quality is acceptable. | `services/hve-evaluator/src/hve_evaluator/layout_candidate.py`, `services/hve-evaluator/src/hve_evaluator/layout_prediction_bundle.py`, `services/hve-evaluator/tests/test_layout_candidate.py`, `services/hve-evaluator/tests/test_layout_prediction_bundle.py`, `docs/operations/hve-layout-evaluator.md` |
| HVE selected-clip visual analysis | **Real internal editor API + worker path, not a dashboard control.** `POST …/clips/:clipId/perception` derives the range from the server-side moment, converges duplicate requests on one deterministic job, and enqueues `analyze_clip_visual`. `GET …/clips/:clipId/perception` returns only user-selectable face-track facts whose dense evidence and own keyframes cover that exact range; it never exposes decoded frames, embeddings, detector internals or S3 locations. The worker seeks to that explicit source range and samples it at a separately bounded dense rate; it persists only source-clock scene/crop facts plus exact range coverage. Typed crop/grid/screen-composite commands bind those returned identities to that same analysis before commit. It never triggers from normal source analysis, does not scan the whole source again, and does not claim active speaker, screen or gameplay recognition. | `services/control-api/src/routes/editor.ts`, `packages/contracts/src/hve-perception.ts`, `packages/contracts/src/hve-editor-commands.ts`, `services/media-worker/src/fourshort_worker/vision.py`, `stages.py`, `services/control-api/src/services/pipeline.ts` |
| HVE render quality admission | **Real media integrity check, not a quality-release gate.** A generated clip is only uploaded after ffprobe verifies expected H.264/yuv420p canvas/FPS, AAC when the source had audio, output duration within a frame plus mux tolerance, file size, and a full FFmpeg decode of all available streams. The same decode pass records bounded output-clock black-segment observations through `blackdetect`; they are explicitly `observation_only` and never invalidate an intentionally dark scene. An FFmpeg image without `blackdetect` falls back to a full decode and records visual QC as unavailable — it never pretends to pass. It still does not certify visual semantics, A/V marker sync, freeze, caption collisions or corpus quality. | `services/media-worker/src/fourshort_worker/media.py` `validate_render`, `stages.py` `render`, `tests/test_render.py` |
| Render artifact cache | **Real, quality-gated within one workspace.** A matching render hash is reusable only if the cached media is retained, not logically deleted and its persisted validation says `valid: true`. Cache reuse creates a fresh clip-version-to-artifact binding and extends retention for the current workspace; it never just marks a clip ready without an artifact. Cross-workspace reuse is forbidden. Legacy/unvalidated cache records are rerendered. | `services/control-api/src/services/pipeline.ts` `face_track` completion branch |
| Subtitle burn-in | **Real for HVE V2 plans** — output-timed cues are burned with ASS/libass after the same time map as video/audio. The current final-render font is the deterministic built-in `HVE Sans` pack (`DejaVu Sans` in the worker image); arbitrary font names and `fontAssetId` fail before a job is queued, so libass cannot silently pick a host fallback. Legacy V1 paths retain their compatibility renderer until V2 is publicly enabled. | `packages/contracts/src/hve-fonts.ts`, `services/control-api/src/services/hve/render-plan.ts`, `services/media-worker/src/fourshort_worker/fonts.py`, `stages.py` |
| HVE render sidecars and project ZIP | **Backend path is real, dashboard control is not exposed yet.** Every worker render with resolved cues uploads SRT and VTT beside its validated MP4. Locally produced artifacts now carry SHA-256 (multipart ETags are not trusted as content hashes); package assembly verifies it when present, verifies exact object byte size, requires scratch for both members and archive, runs ZIP CRC verification before upload, and bounds input/output to 8 GiB / 600 artifacts. The control plane persists sidecars as version-bound `render_artifacts`, and cache reuse binds the retained set rather than losing sidecars. `POST /v1/projects/:projectId/packages` snapshots only currently ready version-pinned artifacts and retry never rerenders or retranscribes. A synthetic H.264 package/extract/full-decode smoke exists; production still needs real S3/control-API integration, browser progress/download UX and corpus release evidence before public advertising. | `db/schema.ts` `projectPackages`, `routes/projects.ts`, `services/pipeline.ts`, `storage.py`, `stages.py` `zip_project`, `tests/test_project_package.py`, `tests/test_project_package_media.py`, `scripts/hve/run-hve3-smoke.mjs` |
| Word-level subtitle timing (active-word highlight, karaoke) | **Real on the sole STT path.** Local Faster-Whisper Large V3 Turbo emits word timestamps and the control API persists them deterministically in `transcript_segments.words`; its configured Silero VAD policy is passed both to transcription and to the persisted speech-interval pass. HVE V2 consumes those canonical words into output-clock caption cues. Legacy V1 burn-in remains segment-level, so its animated preview is not yet a parity claim. There is no SpeechKit/Yandex STT fallback in this repository. | `services/media-worker/src/fourshort_worker/providers.py`, `services/control-api/src/services/transcript.ts`, `packages/contracts/src/hve-time-map.ts`, `services/control-api/src/services/subtitles.ts` |
| Transcript-revision edits (hide/cut/replace word) reaching the render | **Real for both render paths, with an immutable HVE boundary.** Legacy renders replay the chosen append-only revision into subtitle cues. When an initial HVE V2 document is created, `readHveTranscriptSnapshot` deterministically reduces the current revision into per-word display/hide/cut overrides and freezes them in the document; later project-level transcript changes cannot mutate an already queued HVE version. Segment replacements are distributed only across existing timed words (with any tail held by the last word), so the system preserves typed display text without inventing audio timestamps. A subsequent editor draft uses its own immutable document overrides. | `services/control-api/src/services/subtitles.ts`, `services/control-api/src/services/transcript.ts`, `services/control-api/src/services/hve/initial-document.ts`, `services/control-api/src/services/pipeline.ts`, `tests/unit/stt-transcript-parsing.test.ts`, `tests/unit/hve-initial-document.test.ts` |
| Subtitle presets | **The persisted style identity now round-trips.** The picker’s seven values (`clean`, `bold`, `karaoke`, `active_word`, `word_pop`, `minimal_box`, `speaker_colors`) are valid API presets and no longer collapse `active_word`/`word_pop` into `bold`; the renderer still uses `mode` for timing behaviour. `pulse` remains a backwards-compatible stored preset but is not presented in the first picker. HVE V2 visual parity remains corpus-gated. | `app/dashboard/types.ts`, `app/dashboard/components/clip-editor.tsx`, `packages/contracts/src/media.ts`, `services/media-worker/src/fourshort_worker/subtitles.py` |
| 9 `ClipLayout` variants (UI) | `blur_background` and `static_crop`/`solo`/`auto` render (F4 fixed — `static_crop` is now x/y/zoom-aware, not just a dead-centre crop); speaker/screen-tracking modes still fall back to plain centre-crop and stay `LockedField` in the UI | `render.py` `compile_video_filter`, tests in `services/media-worker/tests/test_render.py` |
| Face tracking | **A basic YuNet CPU tracker exists in the worker but is not HVE production active-speaker capability.** The legacy clip stage samples a selected range, assigns short face tracks and emits a smoothed primary crop with static-crop fallback. The new sparse source pass persists face boxes but never associates them with speech. A verified explicit HVE face track can now drive a constant-size crop position trajectory across a resolved output map, including cut boundaries; it does not select a speaker. There is no mouth-motion model, diarization association, scene policy, corpus gate, zoom trajectory or multi-person HVE decision. Do not expose it as active-speaker tracking. | `packages/contracts/src/hve-layout.ts`, `services/control-api/src/services/hve/perception-artifact.ts`, `services/media-worker/src/fourshort_worker/vision.py`, `stages.py` `face_track` / `analyze_visual`, `tests/test_vision.py` |
| HVE active-speaker association core | **Real deterministic association and immutable evidence contract, deliberately not live.** It consumes future diarized speaker turns and compact landmark-derived mouth-motion windows, performs a deterministic one-to-one assignment with coverage/contrast/margin thresholds, rejects overlapping speakers, and returns explicit `insufficient_evidence` or tightly constrained `offscreen` fallback instead of selecting the largest face. `fourshort_worker.active_speaker_evidence` is an executable, strict evaluator boundary: it only accepts bounded documents for the same source hash/duration and rejects raw frames, audio, landmarks and embeddings before compiling `hveActiveSpeakerArtifact`. `services/hve-evaluator` is a separate non-root Python 3.12 candidate image: it pins Sherpa-ONNX 1.13.4 + MediaPipe 0.10.18, refuses an un-hashed/unlicensed three-model local manifest and emits only diarization/mouth/activity/association/resource JSON for evaluator-owned corpus bytes. The evaluator-only HVE-G5 route signs labels and candidate predictions, binds them to the raw manifest and signed S3 index, then calculates per-stratum F1/coverage/offscreen/switch/RSS evidence; promotion requires at least 24 independently sourced items and 120 adjudicated turns in each stratum. No diarization/landmark candidate is in the base worker image and no scene graph currently receives links, so this does **not** make active-speaker tracking live. HVE-G5 additionally requires a signed corpus and Timeweb CPU8/12GB report; absent evidence is `INSUFFICIENT`. | `services/hve-evaluator/`, `packages/contracts/src/hve-perception.ts`, `services/media-worker/src/fourshort_worker/association.py`, `services/media-worker/src/fourshort_worker/active_speaker_evidence.py`, `scripts/hve/active-speaker-evaluation.mjs`, `scripts/hve/evaluate-active-speaker-benchmark.mjs`, `scripts/hve/validate-active-speaker-benchmark.mjs`, `services/media-worker/tests/test_association.py`, `docs/operations/hve-active-speaker-evaluator.md` |
| HVE evaluator prediction-bundle builder | **Real evaluator-only provenance bridge, not a model-quality result.** It consumes a sealed mapping plus immutable candidate folders, re-verifies the exact source hash, diarization/mouth evidence hashes, association artifact hash, model manifest fingerprint and association-code hash, and emits only the existing unsigned `hve-active-speaker-predictions-v1` shape. It requires cgroup-v2 RSS/swap rather than accepting a process-only measurement; separate Ed25519 signing is still required. An actual signed corpus evaluation is still missing, so this cannot unlock automatic tracking. | `services/hve-evaluator/src/hve_evaluator/prediction_bundle.py`, `services/hve-evaluator/tests/test_prediction_bundle.py`, `docs/operations/hve-active-speaker-evaluator.md` |
| Time-range trim | Real, sent as `sourceRange` | `createProjectSchema` |
| Live credit-cost estimate before commit | Client-side arithmetic only, no priced backend endpoint | confirmed absent across `services/control-api/src` |
| Unified credit balance | Real | `GET /v1/billing/summary`, `minuteBuckets` |
| Job pipeline stages shown to user | **3** real distinguishable stages, not 4 (E-AUDIT fix, previously said 4 not 5 — that count was itself wrong) | `new-project-wizard.tsx` `processingStages`/`stageByStatus` |
| Wizard cutting mode (`uniform`/"Простая", `manual`/"Один фрагмент") | **Absent** — `find_moments` has zero mode branching, runs the same AI search+rerank for every mode regardless of which is selected; the picked mode's value reaches the LLM prompt as inert text, nothing acts on it. Locked in the UI (E-AUDIT) | `services/media-worker/src/fourshort_worker/stages.py:156-175` `find_moments`, confirmed no `"manual"`/`"uniform"` branch anywhere in `control-api` or `media-worker` |
| Style round-trip for advanced layouts | **Lossless for persisted styles; compact creation remains intentionally bounded.** `StylePreset` keeps the full `StyleConfig["layout"]` in `layoutConfig`; the human `framing` label is derived display data only. A save after an unrelated caption/brand edit therefore retains `video_image.assetId`, PiP position and screen/gameplay position exactly. The compact styles picker only creates layouts for which it can form a complete configuration (`auto`, active speaker, two speakers, blur and static crop); an existing advanced layout is displayed as editor-owned rather than silently being recreated from a label. Participant assignment and advanced asset selection remain in the clip editor and are not falsely exposed as fully configurable by the compact preset screen. | `app/dashboard/lib/style-layout.ts`, `app/dashboard/types.ts`, `app/dashboard/store.ts`, `app/dashboard/components/styles-view.tsx`, `tests/unit/style-layout.test.ts` |
| Project status values that are actually written | 6 of 12 enum values (`draft`/`uploading`/`importing`/`failed`/`archived` are never set) | `db/schema.ts` project_status enum vs actual writes |
| Hook (opening text overlay) | Absent entirely | no schema field, nothing renders it |
| AI B-roll | Schema string only, dead table, no route/worker/debit | `generative_operations` table, zero writers |
| AI music | Same as B-roll | same |
| Social title/description per clip | Real persistence — hydrated into the editor, accepted by the update contract and written to `clips` | `db/schema.ts` `social_title`/`social_description`, `updateClipSchema`, `routes/projects.ts` |
| Render quality 720p/1080p | Real | `ClipEditorState.exportHeight` |
| 2K/4K | Absent | no such enum value anywhere |
| Custom watermark / outro upload | Absent — `brand_assets` table unreferenced by any route | — |
| Plan-tier export cap / watermark enforcement | **Real and server-enforced** for initial render and clip rerender | `clampExportForPlan` in `packages/product-config`, used by `services/control-api/src/services/pipeline.ts` and `routes/projects.ts` |
| Per-plan storage quota | **Real** — `/v1/storage` returns actual non-deleted, non-expired media usage; multipart creation locks the workspace and reserves declared bytes before issuing signed parts | `services/control-api/src/routes/media.ts`, `routes/uploads.ts`, `packages/product-config` `storageBytes` |
| Project deletion | **Real soft deletion with immediate logical quota recovery** — archives the project, cancels runnable jobs and marks clip artifacts deleted; source/proxy are released only when no other active project uses that source. Physical S3 deletion remains asynchronous | `DELETE /v1/projects/:projectId`, `projects-view.tsx` confirmation dialog |
| Minute-package purchase (T-Bank payment) | **Real and complete** — was the inverse bug: backend has a full T-Bank payment flow (`POST /v1/billing/minute-packages/:code/purchase` creates a real payment, returns a `confirmationUrl`; a webhook at `/v1/billing/tbank/webhook` verifies the notification and credits `minuteBuckets` on success), but `billing-view.tsx`'s "Добавить кредиты" button never called it — it just showed "будет доступна после подключения оплаты" (coming soon) after a fake click handler. Fixed (E-AUDIT) by wiring the button to the real endpoint and redirecting to `confirmationUrl`. Worth re-checking this direction too, not just "UI promises a feature the backend lacks" — a real backend feature the UI hides is just as bad, and costs revenue directly | `services/control-api/src/routes/billing.ts:38-81`, `services/control-api/src/services/tbank.ts`, fixed in `app/dashboard/components/billing-view.tsx` + `app/dashboard/lib/control-api.ts` `purchaseMinutePackage` |
| Plan change (self-serve, with payment) | Absent — only an **admin-only** route exists (`PUT /v1/admin/workspaces/:workspaceId/plan`), no user-facing subscribe/upgrade-with-payment flow. The disabled "Изменить тариф" button + "появится вместе с подключением оплаты" copy is accurate, unlike the minute-package button above — don't conflate the two | `services/control-api/src/routes/admin.ts:271` |
| Wizard's "Мои видео" (reuse a past source) picker | **Could corrupt real project creation** — same failure class as the transcript-boundary bug: `new-project-wizard.tsx` rendered the hardcoded `sourceLibrary` mock array unconditionally, and clicking one of those fake rows set `existingSourceId` to a fake id (`"source-podcast-24"`, etc.) that then flowed straight into the real `createProject({source: {kind: "existing", sourceId: existingSourceId}})` call — sending a nonexistent id to the real backend. A real `GET /v1/sources` route and `listSources()` client already existed and were simply never called. Fixed by fetching real sources into the dialog when `isControlApiConfigured()`, with an honest per-source label built from what `ApiSource` actually has (no title field on the row itself — falls back to the upload's `metadata.originalFileName` or a generic "Видео с YouTube"/"Загруженное видео" by `kind`), while preview/offline mode keeps the mock array unchanged. Found by a background review agent, not by manual reading | `app/dashboard/lib/control-api.ts` `listSources`, `app/dashboard/components/new-project-wizard.tsx` `realSourceLibrary` |
| Clip editor doesn't hydrate layout/subtitle/silence/export from a real saved clip | **Could silently overwrite a real clip's real settings with mock defaults** — worse than a display bug: `state` starts from mock `defaultClipEditorState`, and the load effect only ever patched `title`/`socialTitle`/`socialDescription` from the real `getClip()` response (the rest of `response.version.edl` — real layout, subtitle preset/font/colors, silence removal, export height — was fetched and available but never applied to state, *unless* a matching localStorage draft happened to exist for that exact clip version). `compileEdl()` then unconditionally sends `state.layout`/`state.subtitlePreset`/etc. back through `updateClip()` on save — so opening a real, previously-configured clip on a fresh session (new device, cleared storage) and saving would quietly reset it to mock-preset values. Fixed by parsing `response.version.edl` into the matching `ClipEditorState` fields on load (new `layoutFromApi` in `layout-options.ts`, mirroring the existing `layoutToApi`; a `subtitlePresetFromApi` mirroring `subtitleMode`/`subtitleApiPreset`, same collapsing convention already used by `store.ts`'s `styleFromApi`). Also found in the same pass: the "Нормализовать звук" switch (`state.normalizeAudio`) has no representation anywhere in the EDL schema or `compileEdl()` — a genuine dead control, converted to `LockedField` | `app/dashboard/components/clip-editor.tsx` load effect, `app/dashboard/lib/layout-options.ts` `layoutFromApi` |
| Project transcript drawer's searchable line list (`project-workspace.tsx`, moments-review page) | **Was showing fake demo lines even for real projects, and could silently corrupt real data** — `filteredTranscript` read the static `transcript` array from `data.ts` unconditionally, while the REAL fetched segments (`getTranscript`, populated into `transcriptSegments`) were only used for the edit-text save path, never for what's actually displayed/searched. Worse than a display bug: clicking a line calls `pickBoundary(line.time)`, and "Применить границы" sends those clicked timestamps straight to `updateMoment(...).startMs/endMs` — so a real user clicking on a FAKE demo line's timestamp would silently write that fake timestamp as their real moment's boundary, corrupting real project data with numbers from an unrelated demo podcast. Fixed by enriching the real segments fetch with `time` (from `startMs`), a derived speaker label (segment.speakerId has no real name anywhere in the session data, so distinct ids get generic "Спикер А/Б/…" labels in first-seen order — honest, not fabricated), and per-line "in the active moment" overlap, then switching `filteredTranscript` to prefer real lines whenever `canUseApi` is true. Preview/offline mode is unaffected (still uses the demo array, correctly labelled as such). **Follow-up fix (E-AUDIT, same pass as the `transcript_segments`-never-written discovery):** the original fix's `canUseApi && realTranscriptLines.length ? real : demo` condition still silently fell back to the SAME fake demo dialogue whenever a real connected project had zero segments — which, given the Yandex-provider gap (`transcript_segments` only populated for the OpenAI-Whisper-compatible STT provider, not the default `yandex_speechkit`), is not an edge case but the COMMON case for any unmodified deployment. On top of that, the fetch's `.catch(() => undefined)` swallowed real errors with no notice to the user at all — same silent-failure shape flagged elsewhere in this file. Fixed by never falling back to `demoTranscript` when `canUseApi` is true (only the offline/preview path uses the mock array now), and rendering an honest empty-state message (loading / real error text / "transcript not ready for this project") instead of an empty or fake-populated list | `app/dashboard/components/project-workspace.tsx` `filteredTranscript`, `transcriptError`, `transcriptLoaded`, the `getTranscript` effect |
| Billing transaction history ("История операций") | **Was 100% hardcoded mock data, even in production** — worse than the projects-list gap since there wasn't even a preview/connected split: `billing-view.tsx` unconditionally rendered the static `minuteTransactions` array from `data.ts` regardless of `connection` state, so a real, fully-connected user would still see 4 fake demo transactions forever. The underlying ledger table (`minute_transactions`, written by `reserveMinutes`/`releaseReservation` in `services/minutes.ts` and by admin adjustments) was real and populated, but had zero read route AND the T-Bank purchase webhook only wrote to `minute_buckets`, never inserted a matching ledger row — so even a real endpoint wouldn't have shown purchases. Fixed by: (1) webhook now also inserts a `kind: "grant"` `minute_transactions` row on successful payment; (2) new `GET /v1/billing/transactions` joins `minute_reservations`→`projects` for readable per-charge titles; (3) `listTransactions()` added to `control-api.ts`; (4) `billing-view.tsx` now fetches real data with a loading/error state, falling back to the mock array only when `!isControlApiConfigured()` (offline preview), matching every other page's pattern | `services/control-api/src/routes/billing.ts` `GET /v1/billing/transactions` + tbank webhook, `app/dashboard/components/billing-view.tsx` |
| Projects list ("Мои проекты" page) | **Was 100% hardcoded mock data** — `projects-view.tsx` imported the static `projects` array from `data.ts` directly and never called the backend at all, even though `GET /v1/projects` was already real and working. Every real user saw the same 5 fake demo projects regardless of what they'd actually created — likely the single most visible gap found in the whole E-AUDIT pass, since it's the primary landing list after login. Fixed by: (1) extending the route to join `sources` (kind/duration) and aggregate `clips`/`moment_candidates` counts, since the raw `projects` row alone has none of the fields the list UI needs; (2) adding `listProjects()` to `control-api.ts`; (3) adding `projects` to the shared dashboard store (`store.ts`, same hydrate-on-mount pattern as `styles`/billing, with the existing mock array as the offline-preview fallback); (4) wiring `projects-view.tsx` to read from the store with the same connection notice pattern `styles-view.tsx` already used. Re-verify this row if `projects-view.tsx` or the store's `projectFromApi` mapper changes | `services/control-api/src/routes/projects.ts` `GET /v1/projects`, `app/dashboard/store.ts` `projectFromApi`/hydrate, `app/dashboard/components/projects-view.tsx` |

| Sign-in redirect for a genuinely signed-out visitor | **Was unreachable in practice** — `dashboard-shell.tsx`'s redirect-to-`/sign-in` effect only fired on `connection === "connected" && !user`. But a real signed-out visitor hitting a protected dashboard page causes `listStyles`/`getBillingSummary`/`listProjects` (all workspace-gated) to 401, which lands `hydrate()`'s `catch` block and sets `connection: "error"` — never `"connected"`. So the exact case this redirect exists for never satisfied its own condition: a real signed-out user landed on an honest-but-dead-end "API недоступен / Authentication required" empty page forever, with no visible link to `/sign-in` anywhere in the shell. Only caught by running a real local backend (Postgres + control-api) end-to-end in the browser — signing out and revisiting `/dashboard/projects` reproduced it immediately; it was invisible to code reading and to preview-mode testing, since preview mode never reaches `connection: "error"` at all. Fixed two ways together: (1) `store.ts`'s `hydrate()` now resolves `getSession()` and derives `user` BEFORE the try/catch, so a later failure in the workspace-scoped `Promise.all` can't erase a real signed-in identity into `user: null` — this matters because `connection: "error"` can also mean a transient 500/network blip for a REAL signed-in user, who must not be bounced to sign-in; (2) the redirect effect now checks `connection === "connected" || connection === "error"`, both gated on the now-reliable `!user`. Verified live: real OTP sign-in → real 201 `POST /v1/onboarding/workspace` → sign-out → revisit a protected page → correct redirect to `/sign-in?redirect=...` → fresh OTP → lands back on the originally-requested page, not just `/dashboard` | `app/dashboard/store.ts` `hydrate()`, `app/dashboard/components/dashboard-shell.tsx` redirect effect |
| Projects/styles pages on a real connection error (not preview) | **Was silently showing the fake demo dataset** — `hydrate()`'s catch block did `replace({ ...serverState, connection: "error", ... })`; `serverState.projects`/`.styles` are the SAME mock arrays used for the deliberate offline "preview" case (API not configured at all). So a real, API-configured deployment hitting ANY transient hydration failure (expired session, a 500, a network blip) showed 5 fake demo projects and a fake style library as if they were the user's real data — worse than an empty state, since it's actively wrong content with only a small banner as the sole tell. Fixed by explicitly emptying `styles`/`projects`/`defaultStyleId` in the catch branch instead of inheriting them from `serverState`; `projects-view.tsx` already had a safe empty state, `styles-view.tsx` needed one added (`active` can now be `undefined` when `styles` is empty — every call site was auditied and an early-return empty state added before the JSX that assumed `active` exists). Verified live against a real backend by triggering an actual 401 (no session) | `app/dashboard/store.ts` `hydrate()` catch branch, `app/dashboard/components/styles-view.tsx` empty-state guard |
| New-project processing screen's stage mapping | **Was showing the wrong stage from the moment a project is created** — only found by actually creating a real project against a live backend and reading the real `projects`/`jobs` rows. The backend has exactly 3 distinguishable in-progress statuses: `probing` (set once, at creation, before any job has even run — confirmed by grepping every `projects.status` assignment in `services/control-api/src`), `transcribing` (set when the import/probe job completes; `extract_audio` then runs silently INSIDE this status window with no status of its own before `speech_to_text` starts), and `finding_moments`. The UI displayed 4 stages, so `probing` had been mapped to stage index 1 ("Подготавливаем звук") to leave room for a 4th — meaning every brand-new project showed "preparing audio" immediately after creation, before the source had even been downloaded. `importing`/`uploading`/`draft` (also in the `project_status` enum) are never actually written by any route. Fixed by collapsing the display to 3 stages that each map to one real, unambiguous status (merging the old "Подготавливаем звук" and "Распознаём речь" stages into one honest "Готовим звук и распознаём речь" stage, since nothing distinguishes those two sub-phases from `project.status` alone). Verified live twice: before the fix, a fresh project showed "Этап 2 из 4"; after, the same flow showed "Этап 1 из 3" — correct | `app/dashboard/components/new-project-wizard.tsx` `processingStages`/`stageByStatus`, `services/control-api/src/services/pipeline.ts` `advancePipeline` |
| Project workspace's "Моменты" tab for a real, not-yet-processed project | **Probably the single worst finding of the entire E-AUDIT pass** — `project-workspace.tsx`'s `items` state was initialized to the mock `demoMoments` array, and the load effect only overwrote it with real data `if (apiMoments.length)`. A brand-new or still-processing real project genuinely has zero real moments — that's not an edge case, it's the normal state for the first several minutes of every project's life. So a real user creating a real project and opening it immediately (the single most common thing a user does right after creating a project) saw 5 completely fabricated moments — "Почему первый продукт почти всегда ошибается", "Три сигнала, что пора менять позиционирование", etc., unrelated demo-podcast content — presented as if the AI had already analyzed their video, with real-looking timestamps and a real-looking inspector panel. Two smaller instances of the same class sat right next to it: the header's source line was hardcoded to `YouTube · 01:03:42 · … · исходник оплачен один раз` for every real project regardless of its actual source kind/duration/billing state, and the moment inspector's subtitle preview was hardcoded to a fixed demo phrase instead of the active moment's real title. Only found by creating a real project against a live backend and opening it before any worker had processed its queued job — completely invisible to preview-mode testing (which never has zero mock moments) and to code reading (the `if (apiMoments.length)` guard reads like defensive code, not a bug). Fixed by: (1) making the moments assignment unconditional, so a real empty result is reflected as empty, not silently discarded in favor of stale mock state; (2) adding a real empty-state branch — an honest "Ищем сильные моменты" (spinner) while `project.status` is still `probing`/`transcribing`/`finding_moments`, or "Моменты не найдены" once the search has genuinely concluded with nothing — replacing the demo moment list AND the moment-inspector panel that reads `active.xxx` unconditionally (same "can `active` be undefined now" audit as `styles-view.tsx`'s empty-styles fix); (3) replacing the hardcoded header line with `source.kind`/`source.durationMs` from the real `getProject()` response (`durationMs` can itself be `null` before the probe job runs — shown as "Определяется", not a fake number); (4) replacing the hardcoded subtitle-preview text with the real active moment's title. Verified live: before the fix, a freshly created project (job still `queued`, zero rows in `moment_candidates`) showed 5 fake moments and "01:03:42"; after, it showed a real spinner state, "Определяется", and correct zero counts on both tabs | `app/dashboard/components/project-workspace.tsx` `items`/`sourceInfo`/`projectStatus` state, the `getProject()` load effect, the moments-tab empty-state branch |

## How to re-verify a row yourself

```bash
# Does a route exist for X?
grep -rn "X" services/control-api/src/routes/

# Does the worker actually consume a config field, or silently ignore it?
grep -n "field_name" services/media-worker/src/fourshort_worker/*.py

# Is a DB column ever written, or only read?
grep -rn "column_name:" services/control-api/src/routes/
```

Never take a type definition, a UI mock (`data.ts`), or a schema field as proof a feature works — schemas describe intent; only routes, worker code, and actual writes prove reality.
